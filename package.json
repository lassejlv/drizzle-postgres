{
  "name": "drizzle-postgres",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch .",
    "push": "bun drizzle-kit generate && bun drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4",
    "progress": "^2.0.3"
  }
}
